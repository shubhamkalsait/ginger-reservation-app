apiVersion: v1
kind: Service
metadata:
  name: ginger-reservation-service
spec:
  selector:
    app: ginger-reservation-app
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: NodePort

